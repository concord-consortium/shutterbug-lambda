{
  "name": "shutterbug-lambda",
  "version": "0.1.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/concord-consortium/shutterbug-lambda.git"
  },
  "scripts": {
    "deploy-nochrome": "npm run package-nochrome && echo 'uploading...' && aws lambda update-function-code --function-name arn:aws:lambda:us-east-1:612297603577:function:shutterbug --zip-file fileb://package.zip",
    "deploy": "npm run package && echo 'uploading...' && aws lambda update-function-code --function-name arn:aws:lambda:us-east-1:612297603577:function:shutterbug --zip-file fileb://package.zip",
    "package": "npm run package-prepare && cp chrome/headless_shell.tar.gz dist && cd dist && zip -rq ../package.zip .",
    "package-nochrome": "npm run package-prepare && cd dist && zip -rq ../package.zip .",
    "package-prepare": "npm run babel && cp -r package.json dist && cd dist && PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=1 npm install --production",
    "babel": "rm -rf dist && mkdir dist && ./node_modules/.bin/babel src --out-dir dist",
    "test": "node src/test.js",
    "server": "node src/test-server.js",
    "lint": "standard --fix src/**/*.js"
  },
  "dependencies": {
    "puppeteer": "^0.10.2",
    "tar": "^4.0.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.111.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
